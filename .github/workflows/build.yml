name: Build Pure Stock + KSU-Next
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout Source
        uses: actions/checkout@v4
        with:
          ref: stock

      - name: Install Toolchain & Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential bc zip curl git libssl-dev bison flex libelf-dev gcc-aarch64-linux-gnu gcc-arm-linux-gnueabi libncurses5
          # Tải NDK r21e - Phiên bản chuẩn cho Kernel 4.9 Android Q/R
          wget https://dl.google.com/android/repository/android-ndk-r21e-linux-x86_64.zip
          unzip -q android-ndk-r21e-linux-x86_64.zip

      - name: Purge Old Data & Integrate KSU-Next
        run: |
          rm -rf drivers/kernelsu
          sed -i '/kernelsu/d' drivers/Makefile
          git clone --depth=1 https://github.com/rifsxd/KernelSU-Next KernelSU
          cp -r KernelSU/kernel drivers/kernelsu
          echo "obj-y += kernelsu/" >> drivers/Makefile

      - name: Phẫu thuật mã nguồn (Khắc phục xung đột hệ thống)
        run: |
          # Sửa lỗi DTC yylloc (bắt buộc cho Ubuntu 22.04+)
          sed -i 's/YYLTYPE yylloc;/extern YYLTYPE yylloc;/g' scripts/dtc/dtc-lexer.lex.c_shipped || true
          
          # Fix lỗi sdcardfs Kconfig
          cat << 'EOF' > fs/sdcardfs/Kconfig
          config SDCARDFS_FS
          	tristate "SDCard filesystem support"
          	default y
          	help
          	  Sdcardfs is a stackable wrap-around filesystem.
          EOF
          
          # Inject KSU Hooks
          sed -i '/#include <linux\/syscalls.h>/a #include <linux/kernelsu.h>' fs/read_write.c
          sed -i '/ssize_t ret;/a \n\textern int ksu_handle_vfs_read(struct file **file_ptr, char __user **buf_ptr, size_t *count_ptr, loff_t **pos);\n\tksu_handle_vfs_read(&file, &buf, &count, &pos);' fs/read_write.c
          sed -i '/#include <linux\/compat.h>/a #include <linux/kernelsu.h>' fs/exec.c
          sed -i '/int retval;/a \n\textern int ksu_handle_execveat(int *fd, struct filename **filename_ptr, void *argv, void *envp, int *flags);\n\tksu_handle_execveat(&fd, &filename, &argv, &envp, &flags);' fs/exec.c
          sed -i '/#include <linux\/ima.h>/a #include <linux/kernelsu.h>' fs/open.c
          sed -i '/struct file \*f;/a \n\textern int ksu_handle_faccessat(int *dfd, const char __user **filename_ptr, int *mode, int *flags);\n\tksu_handle_faccessat(&dfd, &filename, &mode, &flags);' fs/open.c

      - name: Configure Kernel
        run: |
          DEFCONFIG=exynos9810-crownlte_defconfig
          CONF_PATH=arch/arm64/configs/$DEFCONFIG
          # Thiết lập cấu hình KSU
          echo "CONFIG_KSU=y" >> $CONF_PATH
          echo "CONFIG_KSU_MANUAL_HOOK=y" >> $CONF_PATH
          # Vô hiệu hóa bảo mật phần cứng Samsung gây xung đột biên dịch
          sed -i 's/CONFIG_CC_STACKPROTECTOR_STRONG=y/CONFIG_CC_STACKPROTECTOR_NONE=y/g' $CONF_PATH
          sed -i 's/CONFIG_KPROBES=y/# CONFIG_KPROBES is not set/g' $CONF_PATH
          sed -i 's/CONFIG_UH_RKP=y/CONFIG_UH_RKP=n/g' $CONF_PATH
          sed -i 's/CONFIG_TIMA=y/CONFIG_TIMA=n/g' $CONF_PATH
          sed -i 's/CONFIG_KNOX_KAP=y/CONFIG_KNOX_KAP=n/g' $CONF_PATH

      - name: Compile Kernel
        run: |
          # Thiết lập đường dẫn Toolchain NDK
          NDK_ROOT=$GITHUB_WORKSPACE/android-ndk-r21e
          CLANG_PATH=$NDK_ROOT/toolchains/llvm/prebuilt/linux-x86_64/bin
          
          # Định nghĩa tham số build
          # Sử dụng CROSS_COMPILE của NDK để đảm bảo tính đồng nhất
          args="-j$(nproc --all) \
                O=out \
                ARCH=arm64 \
                CC=$CLANG_PATH/clang \
                CLANG_TRIPLE=aarch64-linux-android- \
                CROSS_COMPILE=$CLANG_PATH/aarch64-linux-android- \
                CROSS_COMPILE_ARM32=$CLANG_PATH/arm-linux-androideabi- \
                HOSTCFLAGS="-fcommon" \
                HOSTCXXFLAGS="-fcommon" \
                ANDROID_MAJOR_VERSION=10"

          # Chạy cấu hình
          make $args exynos9810-crownlte_defconfig
          
          # Biên dịch chính thức - Nới lỏng kiểm tra để vượt qua code cũ Samsung
          make $args Image.gz-dtb KBUILD_CFLAGS="-Wno-error -Wno-implicit-function-declaration -Wno-int-conversion -Wno-unused-variable"

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Kernel-Note9-NDK-r21e-KSU
          path: out/arch/arm64/boot/Image.gz-dtb
