name: Build Pure Stock + KSU-Next
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source
        uses: actions/checkout@v4  # Nâng cấp lên v4 để đồng bộ
        with:
          ref: stock # Ép buộc sử dụng nhánh stock cho Android 10

      - name: Install Toolchain & Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential bc zip curl git libssl-dev bison flex libelf-dev
          git clone --depth=1 https://github.com/kdrag0n/proton-clang.git clang-llvm

      - name: Purge Old Data & Integrate KSU-Next
        run: |
          # 1. Xóa rác cũ
          rm -rf drivers/kernelsu
          sed -i '/kernelsu/d' drivers/Makefile
          
          # 2. Clone nhánh MAIN (Bản ổn định nhất của KSU-Next hiện tại)
          git clone --depth=1 https://github.com/rifsxd/KernelSU-Next KernelSU -b main
          
          # 3. Trích xuất Hash để kiểm soát phiên bản
          cd KernelSU
          KSU_VER=$(git rev-parse --short HEAD)
          echo "KSU_HASH=$KSU_VER" >> $GITHUB_ENV
          cd ..
          
          # 4. Tích hợp vào Kernel
          cp -r KernelSU/kernel drivers/kernelsu
          echo "obj-y += kernelsu/" >> drivers/Makefile
          echo "Da tich hop KernelSU-Next phien ban: $KSU_VER"

      - name: Apply Manual Hook Patch
        run: |
          # Đảm bảo file ksu_manual_hook.patch đã có ở thư mục gốc của repo
          git apply ksu_manual_hook.patch || { echo "Patch FAIL!"; exit 1; }

      - name: Configure Kernel
        run: |
          DEFCONFIG=exynos9810-crownlte_defconfig
          CONF_PATH=arch/arm64/configs/$DEFCONFIG
          echo "CONFIG_KSU=y" >> $CONF_PATH
          echo "CONFIG_KSU_MANUAL_HOOK=y" >> $CONF_PATH
          sed -i 's/CONFIG_KPROBES=y/# CONFIG_KPROBES is not set/g' $CONF_PATH
          sed -i 's/CONFIG_UH_RKP=y/CONFIG_UH_RKP=n/g' $CONF_PATH
          sed -i 's/CONFIG_TIMA=y/CONFIG_TIMA=n/g' $CONF_PATH
          sed -i 's/CONFIG_KNOX_KAP=y/CONFIG_KNOX_KAP=n/g' $CONF_PATH

      - name: Compile Kernel
        run: |
          export PATH=$GITHUB_WORKSPACE/clang-llvm/bin:$PATH
          args="-j$(nproc --all) O=out ARCH=arm64 CC=clang CROSS_COMPILE=aarch64-linux-gnu- CROSS_COMPILE_ARM32=arm-linux-gnueabi-"
          make $args exynos9810-crownlte_defconfig
          make $args Image.gz-dtb KBUILD_CFLAGS="-Wno-error"

      - name: Upload Artifact
        uses: actions/upload-artifact@v4 # Nâng cấp từ v3 lên v4, BẮT BUỘC phải là v4
        with:
          name: Kernel-Stock-KSU-Next-${{ github.run_number }}
          path: out/arch/arm64/boot/Image.gz-dtb
